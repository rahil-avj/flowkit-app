// Barrel for the FlowLens mode chunk. Loaded via import.meta.glob — presence of
// this file is what makes FlowLens available. Delete the folder to strip it.
export { default } from './components/FlowLensMode'
