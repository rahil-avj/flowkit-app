import type { DevicePreset } from '@platform/types/index'

const device: DevicePreset = {
  label: 'iPad Mini',
  type: 'tablet',
  platform: 'ios',
  width: 744,
  height: 1133,
  cornerRadius: 20,
  screenRadius: 16,
  hasNotch: false,
  safeTop: 24,
  safeBottom: 20,
  safeLeft: 0,
  safeRight: 0,
  marginH: 24,
  minTapTarget: 44,
}

export default device
