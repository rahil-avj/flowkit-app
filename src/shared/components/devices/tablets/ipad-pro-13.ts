import type { DevicePreset } from '@platform/types/index'

const device: DevicePreset = {
  label: 'iPad Pro 13"',
  type: 'tablet',
  platform: 'ios',
  width: 1024,
  height: 1366,
  cornerRadius: 20,
  screenRadius: 16,
  hasNotch: true,
  safeTop: 24,
  safeBottom: 20,
  safeLeft: 0,
  safeRight: 0,
  marginH: 32,
  minTapTarget: 44,
}

export default device
