import type { DevicePreset } from '@platform/types/index'

const device: DevicePreset = {
  label: 'Pixel 9',
  type: 'phone',
  platform: 'android',
  width: 412,
  height: 892,
  cornerRadius: 40,
  screenRadius: 36,
  hasNotch: true,
  safeTop: 48,
  safeBottom: 28,
  safeLeft: 0,
  safeRight: 0,
  marginH: 16,
  minTapTarget: 48,
}

export default device
