import type { DevicePreset } from '@flowkit/types/index'

const device: DevicePreset = {
  label: 'iPhone 16 Pro',
  type: 'phone',
  platform: 'ios',
  width: 393,
  height: 852,
  cornerRadius: 50,
  screenRadius: 44,
  hasNotch: true,
  safeTop: 59,
  safeBottom: 34,
  safeLeft: 0,
  safeRight: 0,
  marginH: 16,
  minTapTarget: 44,
}

export default device
