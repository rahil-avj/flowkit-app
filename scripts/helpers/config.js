// Backward-compat re-export shim only — this file has no logic of its own and is
// NOT authoritative config. Import directly from paths.js or colors.js for new code.
export * from './paths.js'
export * from './colors.js'
